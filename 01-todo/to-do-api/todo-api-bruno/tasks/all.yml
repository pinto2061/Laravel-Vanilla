info:
  name: all
  type: http
  seq: 1

http:
  method: GET
  url: http://to-do-api.test/api/tasks?status=completed&per_page=2
  params:
    - name: page
      value: "2"
      type: query
      disabled: true
    - name: status
      value: completed
      type: query
    - name: per_page
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
